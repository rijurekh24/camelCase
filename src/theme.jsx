import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#01ab81",
    },
    secondary: {
      main: "#104f3f",
    },
    textColor: {
      main: "white",
    },
    backgroundColor: {
      main: "#181818",
      secondary: "#232323",
    },
  },
});
