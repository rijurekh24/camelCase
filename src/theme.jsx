import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#76ABAE",
    },
    secondary: {
      main: "#104f3f",
    },
    textColor: {
      main: "#eeeeee",
      secondary: "#999",
    },
    backgroundColor: {
      main: "#222831",
      secondary: "#31363F",
    },

    borderColor: {
      main: "#222831",
    },
  },
});
